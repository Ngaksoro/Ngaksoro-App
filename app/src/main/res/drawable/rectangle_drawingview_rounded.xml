<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background shape -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#FBFBFB" /> <!-- Set the desired background color -->
            <corners android:radius="12dp" /> <!-- Set the desired corner radius -->
        </shape>
    </item>

    <!-- Inner shadow layer -->
    <item
        android:bottom="2px"
        android:left="2px"
        android:right="2px"
        android:top="2px">
        <shape android:shape="rectangle">
            <corners android:radius="12dp" /> <!-- Set the same corner radius as the background shape -->
        </shape>
    </item>

</layer-list>
